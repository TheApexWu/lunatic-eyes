# Optional: enables AI-powered session intent parsing
# Without this, Lunatic Eyes still works — it uses keyword matching as fallback
# Get your key at https://console.anthropic.com
# ANTHROPIC_API_KEY=sk-ant-...
